export enum ACTIVITY_STATUS {
    ON_TIME = "ON_TIME",
    DELAYED = "DELAYED",
    OVERDUE = "OVERDUE",
}

export enum ACTIVITY_DEPENDENCY_TYPE {
    SF = "SF",
    SS = "SS",
    FS = "FS",
    FF = "FF",
}
